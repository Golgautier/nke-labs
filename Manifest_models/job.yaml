apiVersion: batch/v1
kind: Job
metadata:
  name: <name>
spec:
  template:
    spec:
      containers:
      - name: <name>
        image: <image>
        command: [<comman>,  <arg1>, <arg2>]
      restartPolicy: <mode>
  backoffLimit: <number>